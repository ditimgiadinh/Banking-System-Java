<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.banking</groupId>
	<artifactId>fraud-detection-service</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>

		<!--
          FIX (27/07/2026): THÊM MỚI - spring-cloud.version chưa từng tồn tại
          trong file gốc. fraud-detection-service dùng @FeignClient
          (org.springframework.cloud.openfeign.FeignClient) để gọi sang
          account-service, nhưng thư viện này thuộc Spring Cloud, không thuộc
          Spring Boot, nên cần khai báo version + BOM riêng để tương thích.
        -->
		<spring-cloud.version>2025.1.2</spring-cloud.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
		</dependency>

		<!--
          FIX (27/07/2026): SỬA - artifactId + groupId sai.
          Dòng cũ (giữ lại để đối chiếu):
        -->
		<!--
        <dependency>
           <groupId>org.springframework.boot</groupId>
           <artifactId>spring-boot-starter-kafka</artifactId>
        </dependency>
        -->
		<!--
          Không tồn tại "spring-boot-starter-kafka". Spring Kafka được cung cấp
          bởi module riêng "spring-kafka" thuộc groupId "org.springframework.kafka",
          không phải "org.springframework.boot". Đây là nguyên nhân gây lỗi:
          "cannot find symbol class KafkaListener".
          Dòng mới:
        -->
		<dependency>
			<groupId>org.springframework.kafka</groupId>
			<artifactId>spring-kafka</artifactId>
		</dependency>

		<!--
          FIX (27/07/2026): SỬA - artifactId sai tên.
          Dòng cũ (giữ lại để đối chiếu):
        -->
		<!--
        <dependency>
           <groupId>org.springframework.boot</groupId>
           <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        -->
		<!--
          Không tồn tại "spring-boot-starter-webmvc". Artifact đúng cho REST API
          / Servlet MVC là "spring-boot-starter-web".
          Dòng mới:
        -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<!--
          FIX (27/07/2026): THÊM MỚI - hoàn toàn chưa có trong file gốc.
          Class AccountServiceClient dùng @FeignClient nhưng pom.xml gốc không
          khai báo OpenFeign, gây lỗi:
          "package org.springframework.cloud.openfeign does not exist"
          "cannot find symbol class FeignClient"
        -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-openfeign</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<!--
          FIX (27/07/2026): SỬA/XÓA - Spring Boot không có artifact "*-test"
          riêng cho từng starter. Dòng cũ (giữ lại để đối chiếu):
        -->
		<!--
        <dependency>
           <groupId>org.springframework.boot</groupId>
           <artifactId>spring-boot-starter-actuator-test</artifactId>
           <scope>test</scope>
        </dependency>
        <dependency>
           <groupId>org.springframework.boot</groupId>
           <artifactId>spring-boot-starter-data-redis-test</artifactId>
           <scope>test</scope>
        </dependency>
        <dependency>
           <groupId>org.springframework.boot</groupId>
           <artifactId>spring-boot-starter-kafka-test</artifactId>
           <scope>test</scope>
        </dependency>
        <dependency>
           <groupId>org.springframework.boot</groupId>
           <artifactId>spring-boot-starter-webmvc-test</artifactId>
           <scope>test</scope>
        </dependency>
        -->
		<!--
          Không tồn tại artifact test riêng cho actuator/data-redis/kafka/webmvc.
          Chỉ có DUY NHẤT một artifact test chung cho toàn bộ Spring Boot:
          "spring-boot-starter-test" (JUnit, Mockito, AssertJ, Spring Test).
          Với Kafka, cần thêm riêng "spring-kafka-test" để test KafkaListener.
          Dòng mới:
        -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.kafka</groupId>
			<artifactId>spring-kafka-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<!--
      FIX (27/07/2026): THÊM MỚI - toàn bộ khối dependencyManagement chưa từng
      có trong file gốc. Thiếu khối này khiến Maven không biết version tương
      thích của spring-cloud-starter-openfeign, dễ gây lỗi "version not found"
      hoặc conflict version transitive.
    -->
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>