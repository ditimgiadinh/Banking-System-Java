<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.banking</groupId>
	<artifactId>transaction-service</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>
		<spring-cloud.version>2025.1.2</spring-cloud.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-kafka</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-openfeign</artifactId>
		</dependency>

		<!--
			  FIX (27/07/2026): Thiếu jackson-databind gây lỗi khi start transaction-service.

			  NGUYÊN NHÂN GỐC:
			  Từ Spring Boot 4, starter "spring-boot-starter-web" đã bị đổi tên thành
			  "spring-boot-starter-webmvc" và KHÔNG còn tự động kéo theo Jackson nữa
			  (khác với Spring Boot 3, nơi spring-boot-starter-web luôn mang theo
			  spring-boot-starter-json chứa jackson-databind).

			  Trong Spring Boot 4, Jackson được tách thành starter riêng
			  (spring-boot-starter-jackson) và nâng cấp lên Jackson 3, đổi package
			  từ "com.fasterxml.jackson.*" sang "tools.jackson.*".

			  Tuy nhiên, class JsonSerializer/JsonDeserializer của Spring Kafka
			  (org.springframework.kafka.support.serializer.JsonDeserializer) vẫn
			  dùng API Jackson 2 cũ (com.fasterxml.jackson.databind.JavaType) để
			  serialize/deserialize message. Vì pom.xml gốc chỉ có
			  "spring-boot-starter-webmvc" (không mang Jackson) mà không có thêm
			  Jackson 2 databind nào khác, nên khi KafkaConsumer khởi tạo
			  Deserializers, JVM không tìm thấy class JavaType trên classpath.

			  HẬU QUẢ (chuỗi lỗi thực tế gặp phải):
			  ClassNotFoundException: com.fasterxml.jackson.databind.JavaType
				-> NoClassDefFoundError: com/fasterxml/jackson/databind/JavaType
				  -> KafkaException: Failed to construct kafka consumer
					-> Failed to start bean 'internalKafkaListenerEndpointRegistry'
					  -> Application run failed (context refresh bị cancel toàn bộ)

			  CÁCH SỬA:
			  Thêm trực tiếp dependency jackson-databind (Jackson 2 classic, group
			  com.fasterxml.jackson.core) để cung cấp lại class JavaType mà
			  Spring Kafka JsonSerializer/JsonDeserializer cần. Không cần khai báo
			  version vì đã được spring-boot-starter-parent quản lý qua BOM.

			  LƯU Ý: Cần áp dụng fix tương tự cho account-service, payment-service,
			  fraud-detection-service nếu các service này cũng dùng
			  spring-boot-starter-webmvc + Kafka JsonSerializer/JsonDeserializer.
		-->
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>

		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-kafka-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
